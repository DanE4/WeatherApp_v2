export default class City {
    public name: string;
    public temperature: string;
    public time: string;

    constructor(name: string, temperature: string, time: string) {
        this.name = name;
        this.temperature = temperature;
        this.time = time;
    }
}